<!DOCTYPE html>
<html lang="en">
<head>
    {{> _header}}
    <title>Edit Task</title>
</head>
<body>
  {{> _navbar}}
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-6 col-12">
                <div class="float-right">
                    <a class="text-danger" href="#" data-toggle="modal" data-target="#deleteModal"><i class="fa fa-trash"></i> </a>
                </div>
                <h4>Edit Task</h4><hr/>
        
                {{> _messages }}
                <form>
                    <div class="form-group">
                        <label for="">Title:</label>
                        <input type="text" id="title" name="title" value="{{task.title}}" class="form-control">
                    </div>
                    <div class="form-group">
                        <label for="desctiption">Description:</label>
                        <textarea name="description" id="description" rows="4" class="form-control">{{task.description}}</textarea>
                    </div>
                    <button id="updateBtn" data-id="{{task._id}}" class="btn btn-primary">Update Task</button>
                </form>
            </div>
        </div>
    </div>

    <div class="modal fade" id="deleteModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Delete</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body text-center">
                Are you sure you want to delete <strong>{{task.title}}</strong> ?
                <div class="mt-2 mb-4">
                    <button id="deleteBtn" data-id="{{task._id}}" class="btn btn-danger">Confirm</button>
                </div>
            </div>
            </div>
        </div>
    </div>


    {{> _footer}}
</body>
</html>